// Re-export the QBO callback handler at the path configured in the QuickBooks Developer portal
// The actual implementation lives in /api/qbo/callback/route.ts
export { GET } from "@/app/api/qbo/callback/route";
